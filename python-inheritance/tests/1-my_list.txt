The ``1-my_list`` module
=======================

Using ``MyList``
----------------

Import the class:

    >>> MyList = __import__('1-my_list').MyList

Create list:

    >>> my_list = MyList([3, 1, 4])
    >>> print(my_list)
    [3, 1, 4]

Sorted print:

    >>> my_list.print_sorted()
    [1, 3, 4]

Original list unchanged:

    >>> print(my_list)
    [3, 1, 4]

More tests:

    >>> MyList([6, 9, 2, 3]).print_sorted()
    [2, 3, 6, 9]

    >>> MyList([1, 2, 3]).print_sorted()
    [1, 2, 3]

    >>> MyList([4, 1, 4, 2]).print_sorted()
    [1, 2, 4, 4]

    >>> MyList([-1, -5, 3]).print_sorted()
    [-5, -1, 3]

    >>> MyList([]).print_sorted()
    []
